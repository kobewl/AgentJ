# 本地开发环境配置模板
# 复制此文件为 application-dev.yml 并填入你的实际配置

spring:
  ai:
    dashscope:
      api-key: "你的阿里云 DashScope API Key"
      chat:
        options:
          model: qwen-flash

# 其他本地开发环境特定的配置可以在这里添加